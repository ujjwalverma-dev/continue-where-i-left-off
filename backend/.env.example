# Keep real values in backend/.env (ignored by Git). Never use NEXT_PUBLIC_ for provider keys.
RIME_API_KEY=
QDRANT_URL=
QDRANT_API_KEY=
LLM_API_KEY=
STT_API_KEY=

# Optional local development settings
PORT=4000
FRONTEND_ORIGIN=http://localhost:3000
